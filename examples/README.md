# Examples

Place a few small food images here for quick manual checks.

Recommended smoke test:

```bash
python app/app.py
```

Open `http://127.0.0.1:7860`, upload an image, and verify that the UI returns classification rows, masks, depth, numeric features, and nutrition estimates.
