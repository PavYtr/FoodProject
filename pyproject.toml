[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "food-project-app"
version = "0.1.0"
description = "Gradio inference application for FoodProject"
requires-python = ">=3.11,<3.14"

[tool.setuptools.packages.find]
where = ["src"]
